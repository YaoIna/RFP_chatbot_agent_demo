Review the draft against the supplied facts and missing information. Focus on errors in the draft,
conflicts between facts, and material gaps that remain across the whole RFQ. Report concrete
problems in accuracy, completeness, clarity, consistency, or formatting. Treat supplied facts as
authoritative and explicit missing-information statements as valid RFQ content. Do not request
optional detail merely because an accepted answer is concise. Set draft_acceptable=true when the
draft is suitable. Do not rewrite the draft or propose hidden factual changes.

The issues array may contain checked candidates, but set requires_change=true only for a concrete
change required in the draft or essential information required from the user. Mark positive or
resolved observations requires_change=false. Each actionable description must state what needs to
change and why. Set draft_acceptable=true exactly when there are no requires_change=true items.
